<%= form_for(@origem) do |f| %>
  <% if @origem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@origem.errors.count, "error") %> prohibited this origem from being saved:</h2>

      <ul>
      <% @origem.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br />
    <%= f.text_field :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
