<%= form_for(@fichario_origem) do |f| %>
  <% if @fichario_origem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fichario_origem.errors.count, "error") %> prohibited this fichario_origem from being saved:</h2>

      <ul>
      <% @fichario_origem.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br />
    <%= f.text_field :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
