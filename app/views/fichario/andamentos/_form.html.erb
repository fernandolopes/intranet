<%= form_for(@fichario_andamento) do |f| %>
  <% if @fichario_andamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fichario_andamento.errors.count, "error") %> prohibited this fichario_andamento from being saved:</h2>

      <ul>
      <% @fichario_andamento.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao, "Descrição" %><br />
    <%= f.text_field :descricao %>
  </div>
  <div class="actions">
    <%= f.submit 'Salvar' %>
  </div>
<% end %>
